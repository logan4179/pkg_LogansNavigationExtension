* For LNX_Triangle.V_Center, I need to look into if it would be performant enough to make this a property rather than a cached variable. When making the decision, keep in mind that A) a property would honor the single responsibility principle, and B) 
* In LNX_MeshManipulator, towards the bottom of OnSceneGUI(), where I'm doing the Undo.RecordObject() (this is currently commented out), I think in order to get this working, this record object call should be before the move...
